name: Build and Deploy
on: [push]
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@master

    - name: blog-deploy
      uses: rongminli/blog@master
      env:
        ACCESS_TOKEN: ${{ setting.secrets.ACCESS_TOKEN }}
        TARGET_REPO: rongminli/blog
        TARGET_BRANCH: master
        BUILD_SCRIPT: yarn && yarn docs:build
        BUILD_DIR: docs/.vuepress/dist